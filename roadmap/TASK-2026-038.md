---
pm-task: true
id: TASK-2026-038
title: "md-indexer crate with parser, graph, and CLI"
type: Task
status: done
priority: Critical
sprint: "Sprint 13"
epic: "Core Engine"
assignee: "alice"
created: "2026-05-15T10:00:00Z"
updated: "2026-05-28T16:00:00Z"
due_date: "2026-05-30"
start_date: "2026-05-15"
time_estimate: 3d
time_logged: 2d
progress: 100
dependencies: []
blocks:
  - TASK-2026-040
  - TASK-2026-042
labels:
  - rust
  - parser
  - foundation
---

## Context
Orqestra needs a core Rust library that can parse `roadmap/*.md` files, extract structured task data from YAML frontmatter, build a dependency graph, and expose a CLI for inspection. This is the foundation for everything else — Tauri commands, graph-store, and agent routing all depend on parsed task metadata.

## Acceptance Criteria
- [x] `crates/md-indexer/` with types, parser, graph, indexer, duration modules
- [x] Parse YAML frontmatter with duration support (`8h` → 480 minutes)
- [x] Build dependency graph from `dependencies` and `blocks` fields
- [x] DOT format export for graph visualization
- [x] CLI binary `orqestra deps --format=dot --project-root <path>`
- [x] 37 unit tests all green

## Agent Notes
> Phase 0 output. Duration implemented as u32-minutes newtype with custom serde.
> Phantom nodes for missing dependencies rendered with dashed borders.
> `pulldown-cmark` declared but reserved for future AST parsing.
