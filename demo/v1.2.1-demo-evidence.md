# v1.2.1 Demo Evidence

## Release

- **Tag:** v1.2.1
- **Channel:** public-beta
- **Theme:** Native Git Stabilization

## Stabilization Summary

| Area | Stabilized |
|------|-----------|
| Snapshot parity | 6 new repo states tested (staged+unstaged, added, renamed, nested, ignored, multiple) |
| Risk classification | `.crt`, `.cer`, `*_rsa`, `*_ed25519`, `secrets.*`, `credentials.*`, `.github/actions/` |
| Changed files | Rename entries with `original_path`, porcelain v2 `2 ` prefix, `-u` for individual untracked |
| Commit metadata | Merge commits (2+ parents), multiline messages, unicode metadata |
| Diff/stat | Files with spaces, binary files, deleted files, multiple files, staged-only |
| Symlink safety | Symlinks classified `unknown` risk with explicit reason, never `normal` |
| Manifest | `risk_classification` sub-object with validator enforcement |
| UI | Risk counts, last refresh time, known limitations link |

## Test Count

- **v1.2.0:** 194 passed
- **v1.2.1:** 215 passed (+21)

### New Tests by Area

| Area | New Tests |
|------|-----------|
| Snapshot parity | 6 (staged+unstaged, added, renamed, nested, ignored, multiple) |
| Risk classification | 7 (certs, suffixes, prefixes, actions, symlink, large, binary) |
| Commit metadata | 3 (merge, multiline, unicode) |
| Diff/stat | 5 (spaces, binary, deleted, multiple, staged-only) |

## Risk Classification Matrix

| Path Pattern | Risk | Reason |
|-------------|------|--------|
| `.env`, `.env.*` | secret | env-like path |
| `*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.p8` | secret | secret-like extension |
| `*.crt`, `*.cer` | secret | certificate or credential material path |
| `id_rsa`, `id_ed25519` | secret | secret-like filename |
| `*_rsa`, `*_ed25519` | secret | secret-like filename suffix |
| `secrets.*`, `credentials.*` | secret | credential or secrets file |
| `.github/workflows/**` | workflow | workflow definition path |
| `.github/actions/**` | workflow | reusable action definition path |
| Normal files | normal | (none) |
| Symlinks | unknown | symlink not followed |

## Safety Constraints

| Constraint | Verified |
|------------|---------|
| Secret-risk paths never opened for content | Yes |
| Binary detection reads ≤ 8 KiB | Yes |
| Symlinks not followed | Yes |
| Symlinks never classified `normal` | Yes |
| Large files > 10 MiB classified by metadata only | Yes |
| Diff/stat never includes file contents | Yes |
| All commands report provider + fallback | Yes |
| Rename entries preserve original_path | Yes |

## No-Overclaim Checklist

- [x] `scope` is `read-only`
- [x] `write_operations_migrated` is `false`
- [x] `network_operations_migrated` is `false`
- [x] `fallback_required` is `true`
- [x] `blocking` is `false`
- [x] `secret_safe` is `true`
- [x] `risk_classification.secret_paths` is `path-only`
- [x] `risk_classification.symlink_following` is `false`
- [x] No write/network operations migrated
- [x] Platform status unchanged (Windows + Linux tested)

## Result

**pass**
