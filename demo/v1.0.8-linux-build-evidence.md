# v1.0.8 Linux x64 Build Evidence

**Date:** 2026-06-03
**Source:** CI Run #26847116112 (triggered by v1.0.7 tag)

## CI Job Configuration

- **Runner:** `ubuntu-latest`
- **Platform label:** `orqestra-linux-x64`
- **Steps:** checkout, setup Node, setup Rust, install Linux system deps, npm ci, cargo test, tauri build

## Compile Result

| Step | Result |
|------|--------|
| Checkout | pass |
| Node setup | pass |
| Rust toolchain install | pass |
| Linux system dependencies | pass (gtk3, webkit2gtk, libappindicator, etc.) |
| npm ci | pass |
| cargo test --workspace | pass (141 tests) |
| Vite build (TS) | pass |
| Tauri build (cargo + bundle) | pass |

## Bundle Result

| Artifact Type | Produced? | Notes |
|---------------|-----------|-------|
| `.AppImage` | **No** | Tauri bundler not configured for AppImage target |
| `.deb` | **No** | Tauri bundler not configured for deb target |
| `.rpm` | **No** | Not configured |
| Raw binary | **Yes** | `orqestra-desktop` binary found by smoke check |

The CI smoke check step reported:

> No standard installer artifact found - checking for binary
> Binary found (bundling may not be configured for this platform)

## Assessment

- **compile_status:** pass
- **bundle_status:** not-configured
- **public_artifact:** false
- **smoke_tested:** false
- **Status:** `built-but-unverified`

## What Would Be Required for Promotion

1. Configure Tauri bundler targets for Linux in `tauri.conf.json` (e.g., `"deb"`, `"appimage"`)
2. Produce a standard installer artifact in CI
3. Compute SHA256 of the artifact
4. Run smoke test on a Linux desktop environment
5. Record smoke evidence

## Blocking Issues

- No AppImage/DEB bundle is produced without bundler configuration
- No Linux desktop environment available for smoke testing from Windows
- Tauri Linux bundling requires system dependencies that are installed in CI but the bundler targets are not declared

## Result

Linux x64 compilation is verified in CI. No bundled artifact is produced. Linux remains `built-but-unverified`.
