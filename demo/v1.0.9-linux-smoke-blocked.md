# v1.0.9 Linux Smoke Test -- Blocked

**Date:** 2026-06-03

## Blocker

Linux smoke test was not performed because no Linux desktop environment is available for testing from this Windows workstation.

## What Would Be Required

To promote Linux from `bundle-produced-unverified` to `tested`, the following smoke flow must be run on a Linux desktop:

1. Download `Orqestra_1.0.9_x64.AppImage` from GitHub release
2. Verify SHA256 against `checksums.txt` or `release-manifest.json`
3. `chmod a+x Orqestra_1.0.9_x64.AppImage`
4. Launch AppImage
5. Confirm app window opens
6. Open Orqestra repository
7. Confirm roadmap/task UI loads
8. Confirm dashboard link opens
9. Confirm dashboard shows release metadata
10. Confirm no-key beta mode works
11. Close and relaunch app
12. Record result

## Smoke Flow Result

**Not run.**

Linux remains `bundle-produced-unverified`. This file is reserved for when a maintainer with a Linux desktop can run the smoke flow.

## Recommended Next Step

A maintainer with an Ubuntu desktop (or similar Linux distro with GTK3 and WebKit2GTK) should download the AppImage, run the smoke flow above, and record the result. If all steps pass, Linux can be promoted to `tested`.
