# v1.9.0 Architect Agent as Read-Only Planner Evidence

## Purpose

Complete the agent portfolio with a read-only planning agent.

## Agent Portfolio (after v1.9.0)

| Agent | Status | Mode | Writes Files | Patch Governance |
|-------|--------|------|-------------|-----------------|
| docs-agent | Real | review-only | Yes (via governance) | Yes |
| bugfix-agent | Real | review-only | Yes (via governance) | Yes, symbol-aware |
| architect-agent | Real | read-only-planner | **No** | **No** |
| autonomy | Disabled | — | — | — |

## ArchitectPlanResult DTO

Structurally guaranteed non-patch:
- Has: plan_id, schema_version, summary, context_analysis, proposed_approach, affected_symbols, risk_assessment, dependency_warnings, acceptance_criteria, test_strategy, task_breakdown, adr_draft, confidence
- Does NOT have: before, after, edits, path (as edit target), before_checksum, after_checksum

Cannot be passed to `apply_agent_patch_cmd` — types are incompatible.

## Execution Boundary

```
React UI → Tauri command → Rust builds context → Python AI endpoint
```

No direct frontend-to-AI calls. No AgentRunner owning endpoint construction.

## ADR Context Bounding

Existing ADRs passed as bounded metadata:
- path, title, status
- excerpt: capped at 500 chars
- limit: max 10 ADRs

No unbounded ADR body content in prompt.

## No Runtime Mock

Production behavior:
- Missing AI service → structured error
- No fake plan generated

Test behavior:
- Fixtures may mock HTTP responses
- Production fallback must not produce fake plans

## Tests

- Previous: 410
- New: 10 architect agent tests
- **Total: 427 passed**

### Test Coverage

1. Plan structure validation (all required fields present)
2. No repository mutation (git status unchanged)
3. Missing AI service returns error (no fake plan)
4. Schema version present
5. Confidence bounded 0.0–1.0
6. No patch-shaped fields in DTO (verified via JSON inspection)
7. ADR draft is optional (skip_serializing_if when None)
8. .Orqestra runtime state unchanged
9. Agent context wired (available for real repo)
10. Architect plan is not patch-compatible (no path/checksum fields)

## Files Changed

| File | Change |
|------|--------|
| `services/ai/src/orqestra_ai/architect_agent.py` | +200 LOC (new endpoint) |
| `services/ai/src/orqestra_ai/main.py` | Register route |
| `apps/desktop/src-tauri/src/commands/agents.rs` | +180 LOC (DTOs + command) |
| `apps/desktop/src-tauri/src/main.rs` | Register command |
| `apps/desktop/src/agent/AgentRunner.ts` | Remove mock |
| `apps/desktop/src/components/ArchitectAgentPanel.tsx` | +250 LOC (new component) |
| `apps/desktop/src-tauri/tests/commands_architect.rs` | +250 LOC (10 tests) |
| `apps/desktop/src-tauri/src/lib.rs` | Add commands/diagnostics exports |
| `release-manifest.json` | Added architect_agent section |
| `scripts/validate-release-manifest.ts` | +20 LOC (12 validator gates) |
| `CHANGELOG.md` | v1.9.0 entry |

## Validation

- 427 tests passed
- Manifest validation: PASS
- Desktop TypeScript build: clean
- Tauri build: clean

## Result

**pass**
