# v1.0.8 macOS Build Feasibility Evidence

**Date:** 2026-06-03
**Source:** CI Run #26847116112 (triggered by v1.0.7 tag)

## CI Job Configuration

- **Runner:** `macos-latest`
- **Platform label:** `orqestra-macos-universal`
- **Build target:** `universal-apple-darwin`
- **Steps:** checkout, setup Node, setup Rust (stable + nightly for x86_64), npm ci, cargo test, tauri build --target universal-apple-darwin

## Compile Result

| Step | Result |
|------|--------|
| Checkout | pass |
| Node setup | pass |
| Rust stable toolchain | pass |
| Rust nightly toolchain (for x86_64) | pass |
| rustup target add x86_64-apple-darwin | pass |
| npm ci | pass |
| cargo test --workspace | pass (141 tests) |
| Vite build (TS) | pass |
| Tauri build --target universal-apple-darwin | pass |

## Bundle Result

| Artifact Type | Produced? | Notes |
|---------------|-----------|-------|
| `.dmg` | **No** | Tauri bundler not configured for macOS bundle target |
| `.app.tar.gz` | **No** | Not configured |
| `.app` bundle | **No** | Not produced by CI |
| Raw binary | **Yes** | `orqestra-desktop` binary found by smoke check |

The CI smoke check step reported:

> No standard installer artifact found - checking for binary
> Binary found (bundling may not be configured for this platform)

## Assessment

- **compile_status:** pass (universal-apple-darwin target compiles)
- **bundle_status:** not-configured
- **runner:** macos-latest
- **target:** universal-apple-darwin
- **public_artifact:** false
- **smoke_tested:** false
- **notarized:** false
- **Status:** `build-feasibility-verified`

## What Was Proven

1. Orqestra compiles for `universal-apple-darwin` on GitHub's `macos-latest` runner
2. All 141 Rust tests pass on macOS
3. TypeScript builds pass
4. The raw binary exists after build

## What Was NOT Proven

1. No `.app` bundle or `.dmg` was produced
2. No GUI launch test was performed
3. No notarization was attempted
4. No code signing was performed
5. macOS is not a public beta platform

## What Would Be Required for Promotion

1. Configure Tauri bundler for macOS in `tauri.conf.json` (e.g., `"dmg"`, `"app"`)
2. Produce a standard `.dmg` or `.app.tar.gz` artifact
3. Code sign with Apple Developer ID
4. Notarize via Apple notary service
5. Smoke test on a macOS desktop environment
6. Record smoke evidence

## Result

macOS compilation is verified in CI for the universal-apple-darwin target. No bundled artifact is produced. macOS is `build-feasibility-verified` only, not a supported public beta platform.
